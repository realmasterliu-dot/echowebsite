# TabBar 图标

对齐 H5 Dock：`home` / `compass`(about) / `tags`(pricing) / `envelope`(contact)。

- `{name}.png` — 未选中（灰 `#999999`）
- `{name}-active.png` — 选中（品牌橙 `#FF5500`）

由 `npm run icons:export` 从 Font Awesome 生成；路径已写在 `pages.json` → `tabBar.list`。
