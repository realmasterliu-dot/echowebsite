# 内容区图标（对应 data/icons.js 的 iconKey）

与 H5 同源：`@fortawesome/free-solid-svg-icons`，按需导出为 PNG（品牌色 `#FF5500`）。

重新导出：

```bash
npm run icons:export
```

输出到本目录 `{iconKey}.png`，以及 `../tab/{name}.png` / `{name}-active.png`。
